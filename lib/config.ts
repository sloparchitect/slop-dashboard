// ---------- dashboard configuration ----------
// Customize these values to match your brand / channel identity.

/** Shown in the sidebar nav and browser tab. */
export const APP_NAME = "shorts-dash";

/** Subtitle shown beneath the brand in the sidebar. */
export const APP_TAGLINE = "shorts intelligence";

/** Browser tab title. */
export const PAGE_TITLE = `${APP_NAME} // ${APP_TAGLINE}`;

/**
 * Label for "your own" content in the creator-scope dropdown.
 * This is what appears when no `?creator=` param is set.
 */
export const SELF_LABEL = "My Channel";
